local cmd = hl.exec_cmd
local defaults = require("config/defaults")

hl.on("hyprland.start", function()
    cmd("dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP")
    cmd("systemctl --user import-environment")
    cmd("hash dbus-update-activation-environment 2>/dev/null")
    cmd("dbus-update-activation-environment --systemd")
    cmd("systemctl --user start hyprpolkitagent")
    cmd("systemctl --user start makop")
    cmd("systemctl --user start wob.socket wob")
    cmd("udiskie")
    cmd("gsettings set org.gnome.desktop.interface gtk-theme 'Adwaita-dark' && gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'")
    cmd("pactl set-source-mute @DEFAULT_SOURCE@ true")
    cmd("swaybg -o \\* -i $HOME/Pictures/wallpapers/orange-mountain.png -m fill")
    cmd("hyprlock")
    cmd("solaar --window hide")
    cmd("sleep 10 && waybar")
    cmd("sleep 10 && steam")
    cmd([[sleep 20 && ]].. defaults.homeassistant)
    cmd([[sleep 20 && ]].. defaults.chatapps)
    cmd([[sleep 20 && spotify-launcher -- --enable-features=UseOzonePlatform]])
    cmd("bitwarden-desktop")
end)
