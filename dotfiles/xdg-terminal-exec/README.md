# xdg-terminal-exec

Default terminal emulator configuration for xdg-terminal-exec.

## Decisions

- **kitty** — preferred terminal for Wayland with good GPU rendering
- File is just the terminal name; xdg-terminal-exec finds it via PATH
